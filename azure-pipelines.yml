# Starter pipeline
# Start with a minimal pipeline that you can customize to build and deploy your code.
# Add steps that build, run tests, deploy, and more:
# https://aka.ms/yaml

trigger:
- main

pool:
  vmImage: ubuntu-latest

stages:
  - stage: DeployToDev
    jobs:
      - deployment: Dev
        environment: Dev
        strategy: 
         runOnce:
          deploy:
            steps:
              - checkout: self
              - script: echo "Deployment is running"
           
